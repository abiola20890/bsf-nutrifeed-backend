
version: "3.9"

services:

  api:

    build:

      context: .

      target: development

    container_name: bsf-nutrifeed-api

    ports:

      - "5000:5000"

    env_file:

      - .env

    volumes:

      - .:/app

      - /app/node_modules

    restart: unless-stopped

